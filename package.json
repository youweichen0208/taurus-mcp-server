{
  "name": "taurusdb-data-tools",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace taurusdb-core && npm run build --workspace taurusdb-mcp",
    "check": "npm run check --workspace taurusdb-core && npm run check --workspace taurusdb-mcp",
    "dev": "npm run dev --workspace taurusdb-mcp",
    "start": "npm run start --workspace taurusdb-mcp",
    "test": "npm run build && npm run test --workspace taurusdb-core && npm run test --workspace taurusdb-mcp",
    "cloud:validate": "node scripts/cloud-taurusdb-validate.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
